A simple calculator for jamaican import duty. 

## SETUP

This project is built with React. After cloning, run the commands below to get setup.

### `npm install` to get dependencies

### `npm start` to run the application
